import styled from "styled-components";

const Education = () => {
  const Container = styled.div`
    background: red;
    color: white;
  `;
  return <Container id="eduction">Education</Container>;
};
export default Education;
